version: "3.9"

services:
  test_web:
    build:
      context: .
    container_name: test_web
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/opt/app/
    ports:
      - "8000:8000"
    env_file:
      - .env
